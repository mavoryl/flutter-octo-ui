name: octo_ui
description: >-
  Cross-platform Primer-inspired Flutter UI kit. Optimised for devtools,
  dashboards, and dense data-heavy interfaces.
version: 0.8.5
repository: https://github.com/Autocrab/flutter-octo-ui
issue_tracker: https://github.com/Autocrab/flutter-octo-ui/issues

topics:
  - design-system
  - ui
  - widgets
  - theme
  - dashboard

environment:
  sdk: ^3.6.0
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_octicons: ^1.71.0
  meta: ^1.15.0

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
  golden_matrix: ^0.19.0

flutter:
